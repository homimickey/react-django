# This is a react django template 

- **frontend** is built with `react` and `typescript` and  
- bundled with `parcel`
- **backend** is pure django **NO** `rest_framework`

if you want to add rest framwork feel free to add it 🙂
